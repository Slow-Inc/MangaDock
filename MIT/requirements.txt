networkx
torch
groq
torchvision
torch-summary
einops
scikit-image
# Pin below OpenCV 5: 5.0.0 made cv2.putText strict about CV_8U image depth,
# which breaks the debug-visualization helpers (utils/sort.py) and the render
# goldens in CI. Unpinned, fresh installs (CI) drifted to 5.x. (#359 baseline)
opencv-python>=4.8.0,<5.0.0
pyclipper
shapely
requests
cryptography
freetype-py
aiohttp
tqdm
deepl
ImageHash
Pillow
kornia
backports.cached-property
huggingface_hub
ultralytics  # #170 bubble segmentation (YOLOv8-seg); AGPL-3.0
transformers
diffusers>=0.38  # ADR 003 optional Flux.2 Klein inpainter (MIT_INPAINTER=flux_klein); only loaded when selected
gguf            # ADR 003: load the Flux Klein-4B transformer as a GGUF Q4 quant (diffusers GGUFQuantizationConfig)
jieba           # Chinese word segmentation for line-break parity (non-ENG render): wrap on word, not mid-char
py3langid==0.2.2
sentencepiece
editdistance
numpy>=1.26.4
tensorboardX
websockets
protobuf<6.0.0,>=3.20.2 # `google-genai` dependancy requirement
ctranslate2
colorama
openai==1.63.0
tiktoken
httpx==0.27.2 # stop before blocking change in 0.28.0
safetensors
pandas
onnxruntime
timm
omegaconf
python-dotenv
nest-asyncio
marshmallow
cython
aioshutil
aiofiles
arabic-reshaper
pyhyphen
pythainlp  # Thai word segmentation for correct line-breaking (newmm engine, dict-based, no torch)
langcodes
manga-ocr
langdetect
# #251 build-critical: DenseCRF mask refinement. If this dep is absent the worker
# silently falls back to the raw (un-tightened) mask → faint leftover text residue.
# refine_mask now warns once when missing; keep it in every worker image build.
pydensecrf@https://github.com/lucasb-eyer/pydensecrf/archive/refs/heads/master.zip
accelerate
bitsandbytes
uvicorn
fastapi
pydantic>=2.9.2
python-multipart
google-genai
rich
regex
psutil
--extra-index-url https://frederik-uni.github.io/manga-image-translator-rust/python/wheels/simple/
rusty-manga-image-translator
python-bidi

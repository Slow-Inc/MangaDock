import type { SimScenario, SimNode } from '../engine';

const MIT_NODES: SimNode[] = [
  { id: 'recv',    label: 'HTTP Receive',   sub: 'FastAPI /batch' },
  { id: 'detect',  label: 'Text Detect',    sub: 'manga-ocr detect' },
  { id: 'ocr',     label: 'OCR',            sub: 'manga-ocr read' },
  { id: 'trans',   label: 'Translate',      sub: 'Qwen3 / Gemini' },
  { id: 'inpaint', label: 'Inpaint',        sub: 'LaMa + OpenCV' },
  { id: 'render',  label: 'Render Text',    sub: 'Pillow draw' },
  { id: 'encode',  label: 'Encode Patches', sub: 'base64 PNG' },
  { id: 'hook',    label: 'Webhook',        sub: 'httpx → Backend' },
];

export const mitScenarios: SimScenario[] = [
  {
    id: 'mit-full',
    labelEN: 'Full Pipeline',
    labelTH: 'Pipeline ปกติ',
    badge: 'GPU',
    layout: 'linear',
    nodes: MIT_NODES,
    steps: [
      {
        descEN: 'Worker POSTs image to MIT',
        descTH: 'Worker POST รูปภาพมาที่ MIT',
        techEN: 'Cloudflare Worker sends POST /translate/with-form/patches/batch with the original manga page image (multipart/form-data) + config {target_lang, inpainter: lama_large} + callback_url for the webhook.',
        techTH: 'Cloudflare Worker ส่ง POST /translate/with-form/patches/batch พร้อม original page image (multipart/form-data) + config {target_lang, inpainter: lama_large} + callback_url สำหรับ webhook',
        states: { recv: 'active' },
      },
      {
        descEN: 'Text region detection',
        descTH: 'ตรวจหา text region',
        techEN: 'manga-ocr\'s detector locates text regions in the image. OpenCV preprocessing (noise reduction, binarization) is applied first to improve accuracy on low-resolution scans.',
        techTH: 'manga-ocr detector ตรวจหา text bubble / region ในภาพ — ก่อนหน้านั้น OpenCV ทำ preprocessing (noise reduction, binarization) เพื่อให้แม่นยำขึ้นกับหน้า scan ความละเอียดต่ำ',
        states: { recv: 'ok', detect: 'active' },
      },
      {
        descEN: 'OCR — read text from each region',
        descTH: 'OCR อ่านข้อความจากแต่ละ region',
        techEN: 'manga-ocr reads the Japanese text inside each detected bounding box. Specialized for manga: handles vertical text, handwritten fonts, and furigana that general OCR gets wrong.',
        techTH: 'manga-ocr อ่านข้อความญี่ปุ่นภายใน bounding box แต่ละอันที่ detect ได้ — เชี่ยวชาญ manga โดยเฉพาะ รองรับ vertical text, handwritten font, furigana ที่ general OCR จัดการไม่ถูก',
        states: { recv: 'ok', detect: 'ok', ocr: 'active' },
      },
      {
        descEN: 'Translate OCR text',
        descTH: 'แปลข้อความที่ OCR ได้',
        techEN: 'OCR result is passed to the translator. In local mode (TRANSLATOR_TYPE=local): Qwen3 + CTranslate2 inference on GPU with INT8 quantization. In API mode: Gemini via google-genai client.',
        techTH: 'ส่งผล OCR ให้ translator — Local mode (TRANSLATOR_TYPE=local): Qwen3 + CTranslate2 บน GPU พร้อม INT8 quantization; API mode: Gemini ผ่าน google-genai client',
        states: { recv: 'ok', detect: 'ok', ocr: 'ok', trans: 'active' },
      },
      {
        descEN: 'Inpaint — erase original text',
        descTH: 'Inpaint — ลบ text เดิมออกจากพื้นหลัง',
        techEN: 'LaMa large inpainter (via OpenCV) fills the original text region with a reconstructed background, making it look as if the text was never there. This is the most GPU-intensive step.',
        techTH: 'LaMa large inpainter (ผ่าน OpenCV) เติมพื้นที่ text เดิมด้วย background ที่ reconstruct ขึ้นมา ทำให้ดูราวกับไม่มีข้อความอยู่เลย — ขั้นตอนนี้ใช้ GPU สูงที่สุด',
        states: { recv: 'ok', detect: 'ok', ocr: 'ok', trans: 'ok', inpaint: 'active' },
      },
      {
        descEN: 'Render translated text on inpainted background',
        descTH: 'วาดข้อความที่แปลลงบน inpainted background',
        techEN: 'Pillow draws the translated text on the clean inpainted background for each region. pythainlp (newmm engine) is used to tokenize Thai text before line-breaking so words don\'t split mid-character.',
        techTH: 'Pillow วาดข้อความที่แปลลงบน inpainted background สำหรับแต่ละ region — สำหรับภาษาไทย ใช้ pythainlp (newmm) ตัดคำก่อน line-break เพื่อไม่ให้คำแตกกลางตัวอักษร',
        states: { recv: 'ok', detect: 'ok', ocr: 'ok', trans: 'ok', inpaint: 'ok', render: 'active' },
      },
      {
        descEN: 'Encode patches as base64 PNG',
        descTH: 'Encode patches เป็น base64 PNG',
        techEN: 'Each rendered region is cropped and encoded as a PNG base64 string. The result is a patches[] array where each patch contains {x, y, w, h, img_b64} in pixel coordinates.',
        techTH: 'แต่ละ region ที่ render แล้วถูก crop และ encode เป็น PNG base64 — ผลลัพธ์เป็น array patches[] ที่แต่ละตัวมี {x, y, w, h, img_b64} ในหน่วย pixel',
        states: { recv: 'ok', detect: 'ok', ocr: 'ok', trans: 'ok', inpaint: 'ok', render: 'ok', encode: 'active' },
      },
      {
        descEN: 'POST webhook to Backend',
        descTH: 'POST webhook กลับ Backend',
        techEN: 'httpx sends POST to callback_url with payload {taskId, pageIndex, imgWidth, imgHeight, patches[], error:null}. The request is HMAC-SHA256 signed (X-Webhook-Signature header) so Backend can verify authenticity.',
        techTH: 'httpx ส่ง POST ไปยัง callback_url พร้อม {taskId, pageIndex, imgWidth, imgHeight, patches[], error:null} — request ถูก sign ด้วย HMAC-SHA256 (header X-Webhook-Signature) เพื่อให้ Backend verify ได้',
        states: { recv: 'ok', detect: 'ok', ocr: 'ok', trans: 'ok', inpaint: 'ok', render: 'ok', encode: 'ok', hook: 'active' },
      },
      {
        descEN: 'Webhook delivered — pipeline complete',
        descTH: 'Webhook ส่งสำเร็จ — pipeline เสร็จสิ้น',
        techEN: 'Backend acknowledges the webhook (HTTP 200). MIT marks the task complete. Backend then stores patches in R2 via Cloudflare Worker and caches in L1+L2 Redis for future requests.',
        techTH: 'Backend รับ webhook และตอบ HTTP 200 → MIT mark task complete → Backend เก็บ patches ลง R2 via Worker และ cache ใน L1+L2 Redis สำหรับ request ในอนาคต',
        states: { recv: 'ok', detect: 'ok', ocr: 'ok', trans: 'ok', inpaint: 'ok', render: 'ok', encode: 'ok', hook: 'ok' },
      },
    ],
  },
  {
    id: 'mit-notext',
    labelEN: 'No Text Detected',
    labelTH: 'ไม่พบข้อความ',
    badge: 'NIL',
    layout: 'linear',
    nodes: MIT_NODES,
    steps: [
      {
        descEN: 'Worker POSTs image to MIT',
        descTH: 'Worker POST รูปภาพมาที่ MIT',
        techEN: 'Image received — could be a full-page illustration, title page, or action scene with no speech bubbles.',
        techTH: 'รับภาพเข้ามา — อาจเป็น illustration เต็มหน้า, หน้าชื่อเรื่อง, หรือหน้า action ที่ไม่มี speech bubble',
        states: { recv: 'active' },
      },
      {
        descEN: 'Text detection runs — no regions found',
        descTH: 'ตรวจหา text region — ไม่พบ',
        techEN: 'manga-ocr detector scans the image but finds zero text regions above the confidence threshold. No bounding boxes detected.',
        techTH: 'manga-ocr detector สแกนภาพแต่ไม่พบ text region ใดที่ผ่าน confidence threshold — ไม่มี bounding box เลย',
        states: { recv: 'ok', detect: 'ok' },
      },
      {
        descEN: 'Pipeline short-circuits — skip OCR/translate/inpaint',
        descTH: 'Pipeline ตัดสั้น — ข้าม OCR/แปล/Inpaint',
        techEN: 'No regions to process → OCR, translation, inpainting, and rendering are all skipped immediately. The empty result is fast and incurs zero GPU cost.',
        techTH: 'ไม่มี region ให้ประมวลผล → ข้าม OCR, translate, inpaint, render ทันที — ผลลัพธ์ empty เร็วมาก ไม่เสีย GPU cost เลย',
        states: { recv: 'ok', detect: 'ok', ocr: 'skip', trans: 'skip', inpaint: 'skip', render: 'skip' },
      },
      {
        descEN: 'Encode empty patches array',
        descTH: 'Encode patches array ว่าง',
        techEN: 'patches[] is encoded as an empty array []. This is a valid result — it means this page has no translatable text.',
        techTH: 'patches[] ถูก encode เป็น empty array [] — นี่คือผลลัพธ์ที่ถูกต้อง หมายความว่าหน้านี้ไม่มีข้อความที่แปลได้',
        states: { recv: 'ok', detect: 'ok', ocr: 'skip', trans: 'skip', inpaint: 'skip', render: 'skip', encode: 'active' },
      },
      {
        descEN: 'Webhook delivers empty result',
        descTH: 'Webhook ส่ง empty result กลับ Backend',
        techEN: 'MIT POSTs {taskId, pageIndex, patches: [], error: null} to callback_url. Backend caches this empty result in R2 so future requests for this page skip MIT entirely.',
        techTH: 'MIT POST {taskId, pageIndex, patches: [], error: null} ไปยัง callback_url — Backend cache empty result นี้ลง R2 เพื่อให้ request ในอนาคตสำหรับหน้าเดิมข้าม MIT ทั้งหมด',
        states: { recv: 'ok', detect: 'ok', ocr: 'skip', trans: 'skip', inpaint: 'skip', render: 'skip', encode: 'ok', hook: 'active' },
      },
      {
        descEN: 'Done — Frontend renders original image unchanged',
        descTH: 'เสร็จสิ้น — Frontend แสดงภาพเดิมโดยไม่เปลี่ยนแปลง',
        techEN: 'No patches to overlay. Frontend renders the original page image as-is. The empty result is stored in R2 so this page never runs MIT again.',
        techTH: 'ไม่มี patch ให้ overlay — Frontend แสดงภาพ original ตามเดิม Empty result ถูกเก็บใน R2 ทำให้หน้านี้ไม่รัน MIT ซ้ำอีก',
        states: { recv: 'ok', detect: 'ok', ocr: 'skip', trans: 'skip', inpaint: 'skip', render: 'skip', encode: 'ok', hook: 'ok' },
      },
    ],
  },
  {
    id: 'mit-retry',
    labelEN: 'Webhook Retry',
    labelTH: 'Webhook Retry',
    badge: 'RTY',
    layout: 'linear',
    nodes: MIT_NODES,
    steps: [
      {
        descEN: 'Pipeline completes — patches ready',
        descTH: 'Pipeline เสร็จสิ้น — patches พร้อม',
        techEN: 'All pipeline stages completed successfully. Patches are encoded and ready. MIT now attempts to deliver the webhook to the Backend callback_url.',
        techTH: 'ทุก stage ใน pipeline ทำงานเสร็จแล้ว patches encode เสร็จ — MIT เริ่มส่ง webhook ไปยัง callback_url ของ Backend',
        states: { recv: 'ok', detect: 'ok', ocr: 'ok', trans: 'ok', inpaint: 'ok', render: 'ok', encode: 'ok', hook: 'active' },
      },
      {
        descEN: 'Attempt 1 fails — Backend unreachable',
        descTH: 'ครั้งที่ 1 ล้มเหลว — Backend ไม่ตอบ',
        techEN: 'httpx receives a connection timeout or HTTP 5xx from Backend. server/webhook.py catches the error and schedules a retry with exponential backoff.',
        techTH: 'httpx ได้รับ connection timeout หรือ HTTP 5xx จาก Backend — server/webhook.py catch error และ schedule retry ด้วย exponential backoff',
        states: { recv: 'ok', detect: 'ok', ocr: 'ok', trans: 'ok', inpaint: 'ok', render: 'ok', encode: 'ok', hook: 'err' },
      },
      {
        descEN: 'Retry attempt 2 — after backoff delay',
        descTH: 'Retry ครั้งที่ 2 — หลัง backoff delay',
        techEN: 'After waiting backoff_seconds (exponential), httpx retries the same payload. The retry uses the same HMAC-signed request — Backend can detect duplicates via taskId+pageIndex.',
        techTH: 'หลัง backoff delay (exponential) httpx ส่ง payload เดิมซ้ำอีกครั้ง — HMAC signature เดิม Backend detect duplicate ได้จาก taskId+pageIndex',
        states: { recv: 'ok', detect: 'ok', ocr: 'ok', trans: 'ok', inpaint: 'ok', render: 'ok', encode: 'ok', hook: 'active' },
      },
      {
        descEN: 'Attempt 2 succeeds — webhook delivered',
        descTH: 'ครั้งที่ 2 สำเร็จ — webhook ส่งถึง',
        techEN: 'Backend acknowledges with HTTP 200. MIT marks the task complete. Total result: pipeline succeeded despite temporary Backend unavailability.',
        techTH: 'Backend ตอบ HTTP 200 → MIT mark task complete — ผลสุดท้าย: pipeline สำเร็จแม้ Backend ไม่พร้อมชั่วคราว',
        states: { recv: 'ok', detect: 'ok', ocr: 'ok', trans: 'ok', inpaint: 'ok', render: 'ok', encode: 'ok', hook: 'ok' },
      },
    ],
  },
];

# React Native CLI for Mobile Shell

MangaDock will scaffold the Phase 3 Mobile Shell as a bare React Native CLI TypeScript project under `Mobile/`. Expo was considered because it is faster to start, but React Native CLI better fits the Android-first shell and the later Phase 4 native Android roadmap, including `MediaProjection` and `WindowManager` work.
